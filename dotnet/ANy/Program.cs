
using System.Collections;

string Quicksort(string source) 
{
    var letters = source.ToCharArray();

    for (int i = 0; i < letters.Length; i++)
    {
        
    }
}
var result = Quicksort("dcba");